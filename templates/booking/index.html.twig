{% extends 'base.html.twig' %}

{% block title %}Book a Suite{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<section class="row">
    {#{% for suite in suite %}
        <div class="col-4 mt-4">
            <h2>{{ suite.suiteName }}</h2>
            <p>{{ suite.suitePrice }} €</p>
            {#<a href="{{path("app_bookings_add"), {id:suite.id})}}" class="btn">Add</a>#}
            {#<a href="{{path("app_bookings_add", {id:suite.id}, {id:client.id} )}}" class="btn">Book this suite</a>#}
    {#{% endfor %}#}
   {% for hotel in hotel %}
                <h2>Welcome to the {{ hotel.hotelName }}page!</h2>
            {% endfor %}

    {% for suite in suite %}
        <div class="col-4 mt-4">
            <h2>{{ suite.suiteName }}</h2>
            <p>{{ suite.suitePrice }} € per night.</p>
            <p>Hotel: {{ suite.Hotel }}</p>
            {#{% for hotel in hotel %}
                <a href="{{path("app_hotels", {id: hotel.id}) }}" class="btn">{{ suite.hotel }}</a>
            {% endfor %}#}
            <p>{{ suite.suiteDescription }}</p>
            {#<p>{{ suite.bookings }} </p>#}
            <a href="#" class="btn">Interested? Check availability for this suite!</a>
             
         
    {% endfor %}
</section>
{% endblock %}
